<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ArcadeZone — Unblocked Games</title>
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700;900&family=DM+Sans:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0d0f1a;
    --surface: #161928;
    --surface2: #1e2235;
    --accent: #00e5ff;
    --accent2: #ff4fd8;
    --accent3: #ffe033;
    --text: #f0f2ff;
    --muted: #7a82a8;
    --border: rgba(255,255,255,0.07);
    --card-radius: 14px;
  }
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'DM Sans', sans-serif;
    min-height: 100vh;
  }

  /* HEADER */
  header {
    background: var(--surface);
    border-bottom: 1px solid var(--border);
    padding: 0 2rem;
    position: sticky; top: 0; z-index: 100;
    display: flex; align-items: center; gap: 2rem; height: 64px;
  }
  .logo {
    font-family: 'Orbitron', sans-serif;
    font-size: 1.3rem;
    font-weight: 900;
    color: var(--accent);
    text-shadow: 0 0 20px rgba(0,229,255,0.4);
    white-space: nowrap;
    text-decoration: none;
  }
  .logo span { color: var(--accent2); }
  .search-bar {
    flex: 1; max-width: 480px;
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: 50px;
    padding: 0 1rem 0 1rem;
    display: flex; align-items: center; gap: 8px;
  }
  .search-bar input {
    background: none; border: none; outline: none;
    color: var(--text); font-size: 14px; width: 100%; height: 40px;
    font-family: 'DM Sans', sans-serif;
  }
  .search-bar input::placeholder { color: var(--muted); }
  .search-icon { color: var(--muted); font-size: 16px; }
  nav { display: flex; gap: 4px; margin-left: auto; }
  nav a {
    color: var(--muted); text-decoration: none; font-size: 13px;
    font-weight: 500; padding: 6px 14px; border-radius: 50px;
    transition: all 0.2s;
  }
  nav a:hover { color: var(--text); background: var(--surface2); }
  nav a.active { color: var(--accent); background: rgba(0,229,255,0.1); }

  /* HERO */
  .hero {
    background: linear-gradient(135deg, #0d0f1a 0%, #1a0d2e 50%, #0d1a2e 100%);
    padding: 3rem 2rem 2rem;
    text-align: center;
    position: relative; overflow: hidden;
  }
  .hero::before {
    content: '';
    position: absolute; inset: 0;
    background: radial-gradient(ellipse at 30% 50%, rgba(0,229,255,0.06) 0%, transparent 60%),
                radial-gradient(ellipse at 70% 50%, rgba(255,79,216,0.06) 0%, transparent 60%);
    pointer-events: none;
  }
  .hero h1 {
    font-family: 'Orbitron', sans-serif;
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 900;
    background: linear-gradient(90deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.75rem;
  }
  .hero p { color: var(--muted); font-size: 1.05rem; max-width: 500px; margin: 0 auto 2rem; }
  .hero-cats { display: flex; gap: 10px; justify-content: center; flex-wrap: wrap; }
  .cat-pill {
    background: var(--surface2); border: 1px solid var(--border);
    color: var(--muted); padding: 8px 18px; border-radius: 50px;
    font-size: 13px; cursor: pointer; transition: all 0.2s; font-weight: 500;
  }
  .cat-pill:hover, .cat-pill.active {
    border-color: var(--accent); color: var(--accent);
    background: rgba(0,229,255,0.08);
  }

  /* MAIN LAYOUT */
  .layout { display: flex; gap: 0; max-width: 1400px; margin: 0 auto; }

  /* SIDEBAR */
  aside {
    width: 220px; min-width: 220px;
    background: var(--surface);
    border-right: 1px solid var(--border);
    padding: 1.5rem 1rem;
    position: sticky; top: 64px; height: calc(100vh - 64px); overflow-y: auto;
  }
  .sidebar-section { margin-bottom: 2rem; }
  .sidebar-label {
    font-size: 10px; font-weight: 600; letter-spacing: 0.12em;
    color: var(--muted); text-transform: uppercase; margin-bottom: 0.75rem;
    padding: 0 8px;
  }
  .sidebar-item {
    display: flex; align-items: center; gap: 10px;
    padding: 9px 12px; border-radius: 10px; cursor: pointer;
    font-size: 13px; font-weight: 500; color: var(--muted);
    transition: all 0.2s; text-decoration: none;
  }
  .sidebar-item:hover { background: var(--surface2); color: var(--text); }
  .sidebar-item.active { background: rgba(0,229,255,0.1); color: var(--accent); }
  .sidebar-item .icon { font-size: 16px; width: 20px; text-align: center; }

  /* MAIN CONTENT */
  main { flex: 1; padding: 2rem; overflow: hidden; }

  .section-header {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 1.25rem;
  }
  .section-title {
    font-family: 'Orbitron', sans-serif;
    font-size: 0.9rem; font-weight: 700; color: var(--text);
    display: flex; align-items: center; gap: 8px;
  }
  .section-title::before {
    content: ''; display: block; width: 3px; height: 18px;
    background: var(--accent); border-radius: 2px;
  }
  .see-all { font-size: 12px; color: var(--muted); cursor: pointer; }
  .see-all:hover { color: var(--accent); }

  /* FEATURED BANNER */
  .featured-banner {
    background: linear-gradient(135deg, #1a1033 0%, #0d2033 100%);
    border: 1px solid rgba(0,229,255,0.15);
    border-radius: var(--card-radius);
    padding: 2rem;
    margin-bottom: 2.5rem;
    display: flex; align-items: center; gap: 2rem;
    position: relative; overflow: hidden; cursor: pointer;
    transition: border-color 0.2s;
  }
  .featured-banner:hover { border-color: rgba(0,229,255,0.4); }
  .featured-banner::after {
    content: 'FEATURED';
    position: absolute; top: 14px; right: 14px;
    font-size: 9px; font-weight: 700; letter-spacing: 0.15em;
    color: var(--accent3); background: rgba(255,224,51,0.1);
    padding: 4px 10px; border-radius: 50px;
    border: 1px solid rgba(255,224,51,0.2);
  }
  .featured-art {
    width: 120px; height: 120px; border-radius: 16px;
    flex-shrink: 0; display: flex; align-items: center; justify-content: center;
    font-size: 4rem;
    background: linear-gradient(135deg, #1e3a5f, #2d1b4e);
    border: 1px solid rgba(0,229,255,0.2);
  }
  .featured-info h2 { font-family: 'Orbitron', sans-serif; font-size: 1.4rem; margin-bottom: 0.5rem; }
  .featured-info p { color: var(--muted); font-size: 0.9rem; line-height: 1.5; max-width: 400px; margin-bottom: 1rem; }
  .play-btn {
    background: var(--accent); color: #000; font-weight: 700;
    padding: 10px 24px; border-radius: 50px; border: none;
    cursor: pointer; font-size: 13px; font-family: 'DM Sans', sans-serif;
    transition: all 0.2s; display: inline-flex; align-items: center; gap: 6px;
  }
  .play-btn:hover { background: #33eaff; transform: scale(1.03); }

  /* GAME GRID */
  .games-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
    gap: 16px;
    margin-bottom: 2.5rem;
  }
  .game-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--card-radius);
    overflow: hidden; cursor: pointer;
    transition: transform 0.2s, border-color 0.2s, box-shadow 0.2s;
    text-decoration: none; display: block;
  }
  .game-card:hover {
    transform: translateY(-4px);
    border-color: rgba(0,229,255,0.3);
    box-shadow: 0 8px 30px rgba(0,0,0,0.4);
  }
  .game-thumb {
    width: 100%; aspect-ratio: 4/3;
    display: flex; align-items: center; justify-content: center;
    font-size: 3rem; position: relative;
  }
  .game-badge {
    position: absolute; top: 8px; right: 8px;
    font-size: 9px; font-weight: 700; letter-spacing: 0.1em;
    padding: 3px 8px; border-radius: 50px;
  }
  .badge-new { background: rgba(0,229,255,0.15); color: var(--accent); border: 1px solid rgba(0,229,255,0.25); }
  .badge-hot { background: rgba(255,79,216,0.15); color: var(--accent2); border: 1px solid rgba(255,79,216,0.25); }
  .badge-top { background: rgba(255,224,51,0.15); color: var(--accent3); border: 1px solid rgba(255,224,51,0.25); }
  .game-info { padding: 10px 12px 12px; }
  .game-title { font-size: 13px; font-weight: 600; margin-bottom: 4px; }
  .game-meta { font-size: 11px; color: var(--muted); display: flex; justify-content: space-between; }
  .stars { color: var(--accent3); }

  /* GAME MODAL */
  .modal-overlay {
    display: none; position: fixed; inset: 0;
    background: rgba(0,0,0,0.85); z-index: 1000;
    align-items: center; justify-content: center;
    backdrop-filter: blur(6px);
  }
  .modal-overlay.open { display: flex; }
  .modal {
    background: var(--surface);
    border: 1px solid rgba(0,229,255,0.2);
    border-radius: 20px;
    width: min(96vw, 1000px);
    max-height: 90vh;
    overflow: hidden;
    display: flex; flex-direction: column;
    box-shadow: 0 40px 80px rgba(0,0,0,0.6);
  }
  .modal-header {
    display: flex; align-items: center; gap: 14px;
    padding: 16px 20px;
    border-bottom: 1px solid var(--border);
  }
  .modal-title { font-family: 'Orbitron', sans-serif; font-size: 0.95rem; flex: 1; }
  .modal-close {
    background: var(--surface2); border: 1px solid var(--border);
    color: var(--muted); border-radius: 8px; padding: 6px 12px;
    cursor: pointer; font-size: 13px; transition: all 0.2s;
  }
  .modal-close:hover { color: var(--text); border-color: rgba(255,255,255,0.2); }
  .modal-body {
    flex: 1; display: flex; flex-direction: column;
    min-height: 0;
  }
  .game-frame-wrap {
    flex: 1; background: #000;
    display: flex; align-items: center; justify-content: center;
    min-height: 400px;
  }
  .game-frame-wrap iframe {
    width: 100%; height: 520px; border: none;
  }
  .game-frame-placeholder {
    text-align: center; padding: 3rem;
  }
  .game-frame-placeholder .big-icon { font-size: 4rem; margin-bottom: 1rem; }
  .game-frame-placeholder h3 { font-family: 'Orbitron', sans-serif; font-size: 1.1rem; margin-bottom: 0.5rem; }
  .game-frame-placeholder p { color: var(--muted); font-size: 13px; margin-bottom: 1.5rem; }
  .modal-footer {
    padding: 12px 20px;
    border-top: 1px solid var(--border);
    display: flex; gap: 10px; align-items: center;
  }
  .modal-tag {
    background: var(--surface2); border: 1px solid var(--border);
    font-size: 11px; color: var(--muted); padding: 4px 10px; border-radius: 50px;
  }
  .fullscreen-btn {
    margin-left: auto; background: none; border: 1px solid var(--border);
    color: var(--muted); padding: 6px 14px; border-radius: 8px; cursor: pointer;
    font-size: 12px; transition: all 0.2s;
  }
  .fullscreen-btn:hover { color: var(--text); border-color: rgba(255,255,255,0.2); }

  /* FOOTER */
  footer {
    background: var(--surface);
    border-top: 1px solid var(--border);
    padding: 2rem;
    text-align: center;
    color: var(--muted);
    font-size: 13px;
  }
  footer a { color: var(--accent); text-decoration: none; }

  /* SEARCH OVERLAY */
  .search-results {
    display: none; position: absolute; top: 64px; left: 0; right: 0;
    background: var(--surface2); border-bottom: 1px solid var(--border);
    padding: 1rem 2rem; z-index: 99;
  }
  .search-results.open { display: block; }
  .sr-grid { display: flex; gap: 12px; flex-wrap: wrap; }
  .sr-item {
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 10px; padding: 10px 14px; cursor: pointer;
    display: flex; align-items: center; gap: 10px; transition: all 0.2s;
    font-size: 13px; font-weight: 500;
  }
  .sr-item:hover { border-color: rgba(0,229,255,0.3); color: var(--accent); }

  @media (max-width: 900px) {
    aside { display: none; }
    .hero { padding: 2rem 1rem 1.5rem; }
    main { padding: 1rem; }
    header { padding: 0 1rem; }
    nav { display: none; }
  }
</style>
</head>
<body>

<header>
  <a href="#" class="logo">Arcade<span>Zone</span></a>
  <div class="search-bar">
    <span class="search-icon">🔍</span>
    <input type="text" id="searchInput" placeholder="Search games..." autocomplete="off">
  </div>
  <nav>
    <a href="#" class="active">Home</a>
    <a href="#">New Games</a>
    <a href="#">Top Rated</a>
    <a href="#">2 Player</a>
  </nav>
</header>

<div class="search-results" id="searchResults">
  <div class="sr-grid" id="srGrid"></div>
</div>

<div class="hero">
  <h1>Play Free Online Games</h1>
  <p>100% unblocked — no downloads, no installs. Jump in and play instantly.</p>
  <div class="hero-cats">
    <div class="cat-pill active" onclick="filterCat('all',this)">🎮 All Games</div>
    <div class="cat-pill" onclick="filterCat('racing',this)">🏎️ Racing</div>
    <div class="cat-pill" onclick="filterCat('puzzle',this)">🧩 Puzzle</div>
    <div class="cat-pill" onclick="filterCat('action',this)">⚔️ Action</div>
    <div class="cat-pill" onclick="filterCat('sports',this)">⚽ Sports</div>
    <div class="cat-pill" onclick="filterCat('io',this)">🌐 .io Games</div>
    <div class="cat-pill" onclick="filterCat('retro',this)">👾 Retro</div>
  </div>
</div>

<div class="layout">
  <aside>
    <div class="sidebar-section">
      <div class="sidebar-label">Categories</div>
      <div class="sidebar-item active" onclick="filterCat('all',this)"><span class="icon">🎮</span> All Games</div>
      <div class="sidebar-item" onclick="filterCat('racing',this)"><span class="icon">🏎️</span> Racing</div>
      <div class="sidebar-item" onclick="filterCat('puzzle',this)"><span class="icon">🧩</span> Puzzle</div>
      <div class="sidebar-item" onclick="filterCat('action',this)"><span class="icon">⚔️</span> Action</div>
      <div class="sidebar-item" onclick="filterCat('sports',this)"><span class="icon">⚽</span> Sports</div>
      <div class="sidebar-item" onclick="filterCat('io',this)"><span class="icon">🌐</span> .io Games</div>
      <div class="sidebar-item" onclick="filterCat('retro',this)"><span class="icon">👾</span> Retro</div>
    </div>
    <div class="sidebar-section">
      <div class="sidebar-label">Popular Tags</div>
      <div class="sidebar-item"><span class="icon">🔥</span> Trending</div>
      <div class="sidebar-item"><span class="icon">⭐</span> Top Rated</div>
      <div class="sidebar-item"><span class="icon">✨</span> New Arrivals</div>
      <div class="sidebar-item"><span class="icon">👥</span> Multiplayer</div>
    </div>
  </aside>

  <main id="mainContent">
    <!-- Featured Banner -->
    <div class="featured-banner" onclick="openGame('2048')">
      <div class="featured-art">🔢</div>
      <div class="featured-info">
        <h2>2048</h2>
        <p>Slide tiles to combine numbers and reach the legendary 2048 tile. Simple rules, infinite challenge — the classic brain-melting puzzle everyone loves.</p>
        <button class="play-btn">▶ Play Now</button>
      </div>
    </div>

    <!-- Featured Section -->
    <div class="section-header">
      <div class="section-title">Featured Games</div>
      <span class="see-all">See all →</span>
    </div>
    <div class="games-grid" id="gamesGrid"></div>

    <div class="section-header">
      <div class="section-title">New &amp; Trending</div>
      <span class="see-all">See all →</span>
    </div>
    <div class="games-grid" id="trendingGrid"></div>
  </main>
</div>

<!-- Game Modal -->
<div class="modal-overlay" id="modalOverlay" onclick="closeModalOutside(event)">
  <div class="modal">
    <div class="modal-header">
      <div class="modal-title" id="modalTitle">Game Title</div>
      <button class="modal-close" onclick="closeModal()">✕ Close</button>
    </div>
    <div class="modal-body">
      <div class="game-frame-wrap" id="gameFrameWrap"></div>
    </div>
    <div class="modal-footer" id="modalFooter">
      <span class="modal-tag" id="modalTag">Category</span>
      <button class="fullscreen-btn" onclick="goFullscreen()">⛶ Fullscreen</button>
    </div>
  </div>
</div>

<footer>
  <p>🎮 <strong>ArcadeZone</strong> — Free Unblocked Games. No login required. <a href="#">Privacy</a> · <a href="#">About</a></p>
</footer>

<script>
const games = [
  { id: '2048', title: '2048', emoji: '🔢', cat: 'puzzle', badge: 'top', rating: '4.9', plays: '2.1M',
    desc: 'Combine tiles to reach 2048!',
    url: 'https://play2048.co/', tags: ['Puzzle','Numbers','Strategy'] },
  { id: 'tetris', title: 'Jstris', emoji: '🟦', cat: 'puzzle', badge: 'hot', rating: '4.8', plays: '1.8M',
    desc: 'Fast-paced competitive Tetris!',
    url: 'https://jstris.jezevec10.com/', tags: ['Puzzle','Retro','Classic'] },
  { id: 'snake', title: 'Snake', emoji: '🐍', cat: 'retro', badge: 'new', rating: '4.5', plays: '900K',
    desc: 'Classic snake, eat & grow!',
    url: 'https://www.google.com/fbx?fbx=snake_arcade', tags: ['Retro','Classic','Arcade'] },
  { id: 'flappy', title: 'Flappy Bird', emoji: '🐦', cat: 'action', badge: null, rating: '4.3', plays: '3.2M',
    desc: 'Tap to fly, avoid the pipes!',
    url: 'https://flappybird.io/', tags: ['Action','Casual','Hard'] },
  { id: 'chess', title: 'Chess', emoji: '♟️', cat: 'puzzle', badge: 'top', rating: '4.9', plays: '5.0M',
    desc: 'Classic chess vs AI.',
    url: 'https://www.chess.com/play/computer', tags: ['Strategy','Puzzle','Classic'] },
  { id: 'agar', title: 'Agar.io', emoji: '🔵', cat: 'io', badge: 'hot', rating: '4.7', plays: '8.4M',
    desc: 'Eat cells, grow, dominate!',
    url: 'https://agar.io/', tags: ['.io','Multiplayer','Action'] },
  { id: 'slither', title: 'Slither.io', emoji: '🐛', cat: 'io', badge: null, rating: '4.6', plays: '6.1M',
    desc: 'Snake meets multiplayer chaos.',
    url: 'https://slither.io/', tags: ['.io','Multiplayer','Casual'] },
  { id: 'krunker', title: 'Krunker.io', emoji: '🔫', cat: 'io', badge: 'hot', rating: '4.8', plays: '4.3M',
    desc: 'Browser FPS — pixel shooter!',
    url: 'https://krunker.io/', tags: ['.io','Shooter','Multiplayer'] },
  { id: 'minesweep', title: 'Minesweeper', emoji: '💣', cat: 'puzzle', badge: null, rating: '4.4', plays: '1.2M',
    desc: 'Classic MS Minesweeper.',
    url: 'https://minesweeper.online/', tags: ['Puzzle','Classic','Logic'] },
  { id: 'wordle', title: 'Wordle', emoji: '🟩', cat: 'puzzle', badge: 'new', rating: '4.7', plays: '2.8M',
    desc: 'Guess the 5-letter word!',
    url: 'https://www.nytimes.com/games/wordle/index.html', tags: ['Puzzle','Word','Daily'] },
  { id: 'neon-racing', title: 'Drifting Games', emoji: '🏎️', cat: 'racing', badge: 'new', rating: '4.5', plays: '750K',
    desc: 'Neon drift racing action.',
    url: 'https://www.crazygames.com/game/drift-hunters', tags: ['Racing','Driving','Neon'] },
  { id: 'soccer', title: 'Soccer Skills', emoji: '⚽', cat: 'sports', badge: null, rating: '4.3', plays: '540K',
    desc: 'Dribble, shoot, score goals!',
    url: 'https://www.crazygames.com/game/soccer-skills-world-cup', tags: ['Sports','Soccer','Skill'] },
  { id: 'basketball', title: 'Basketball IO', emoji: '🏀', cat: 'sports', badge: 'hot', rating: '4.5', plays: '820K',
    desc: 'Shoot hoops in real-time!',
    url: 'https://basketbros.io/', tags: ['Sports','Basketball','.io'] },
  { id: 'stickman', title: 'Stickman Archer', emoji: '🏹', cat: 'action', badge: null, rating: '4.2', plays: '430K',
    desc: 'Arrow precision combat.',
    url: 'https://www.crazygames.com/game/stickman-archer-3', tags: ['Action','Skill','Stickman'] },
  { id: 'pacman', title: 'Pac-Man', emoji: '🟡', cat: 'retro', badge: 'top', rating: '4.8', plays: '4.5M',
    desc: 'Eat dots, dodge ghosts!',
    url: 'https://www.google.com/search?q=play+pac+man', tags: ['Retro','Classic','Arcade'] },
  { id: 'drift', title: 'Moto Racing', emoji: '🏍️', cat: 'racing', badge: null, rating: '4.4', plays: '670K',
    desc: 'Bike stunts & obstacle race.',
    url: 'https://www.crazygames.com/game/moto-x3m', tags: ['Racing','Bike','Stunts'] },
];

const trendingIds = ['krunker','agar','basketball','drift','wordle','flappy'];
const trending = trendingIds.map(id => games.find(g => g.id === id)).filter(Boolean);

function bgGrad(cat) {
  const map = {
    puzzle:'linear-gradient(135deg,#1a2240,#0d2030)',
    retro:'linear-gradient(135deg,#2a1040,#0a1a10)',
    action:'linear-gradient(135deg,#301010,#201030)',
    racing:'linear-gradient(135deg,#102030,#201010)',
    sports:'linear-gradient(135deg,#0a2010,#102020)',
    io:'linear-gradient(135deg,#102020,#202010)',
  };
  return map[cat] || 'linear-gradient(135deg,#161928,#1a1633)';
}

function renderGrid(containerId, list) {
  const grid = document.getElementById(containerId);
  grid.innerHTML = list.map(g => `
    <div class="game-card" onclick="openGame('${g.id}')">
      <div class="game-thumb" style="background:${bgGrad(g.cat)}">
        <span style="font-size:3rem">${g.emoji}</span>
        ${g.badge ? `<span class="game-badge badge-${g.badge}">${g.badge.toUpperCase()}</span>` : ''}
      </div>
      <div class="game-info">
        <div class="game-title">${g.title}</div>
        <div class="game-meta">
          <span class="stars">★</span><span> ${g.rating}</span>
          <span>${g.plays}</span>
        </div>
      </div>
    </div>
  `).join('');
}

renderGrid('gamesGrid', games);
renderGrid('trendingGrid', trending);

let currentGame = null;

function openGame(id) {
  const g = games.find(x => x.id === id);
  if (!g) return;
  currentGame = g;
  document.getElementById('modalTitle').textContent = '▶ ' + g.title;
  document.getElementById('modalTag').textContent = g.cat.toUpperCase();
  const wrap = document.getElementById('gameFrameWrap');
  wrap.innerHTML = `
    <div class="game-frame-placeholder">
      <div class="big-icon">${g.emoji}</div>
      <h3>${g.title}</h3>
      <p>${g.desc}</p>
      <button class="play-btn" onclick="launchFrame('${g.id}')">▶ Launch Game</button>
      <p style="margin-top:12px;font-size:11px;color:var(--muted)">Opens in a new tab if blocked by your browser.</p>
    </div>
  `;
  document.getElementById('modalOverlay').classList.add('open');
  document.body.style.overflow = 'hidden';
}

function launchFrame(id) {
  const g = games.find(x => x.id === id);
  if (!g) return;
  const wrap = document.getElementById('gameFrameWrap');
  wrap.innerHTML = `<iframe src="${g.url}" allowfullscreen></iframe>`;
}

function closeModal() {
  document.getElementById('modalOverlay').classList.remove('open');
  document.getElementById('gameFrameWrap').innerHTML = '';
  document.body.style.overflow = '';
}

function closeModalOutside(e) {
  if (e.target === document.getElementById('modalOverlay')) closeModal();
}

function goFullscreen() {
  const frame = document.querySelector('#gameFrameWrap iframe');
  if (frame && frame.requestFullscreen) frame.requestFullscreen();
  else if (currentGame) window.open(currentGame.url, '_blank');
}

function filterCat(cat, el) {
  document.querySelectorAll('.cat-pill,.sidebar-item').forEach(x => x.classList.remove('active'));
  if (el) el.classList.add('active');
  const filtered = cat === 'all' ? games : games.filter(g => g.cat === cat);
  renderGrid('gamesGrid', filtered);
  const tFiltered = cat === 'all' ? trending : trending.filter(g => g.cat === cat);
  renderGrid('trendingGrid', tFiltered);
}

const searchInput = document.getElementById('searchInput');
const searchResults = document.getElementById('searchResults');
const srGrid = document.getElementById('srGrid');

searchInput.addEventListener('input', function() {
  const q = this.value.trim().toLowerCase();
  if (!q) { searchResults.classList.remove('open'); return; }
  const matches = games.filter(g => g.title.toLowerCase().includes(q) || g.tags.some(t => t.toLowerCase().includes(q)));
  srGrid.innerHTML = matches.length ? matches.map(g => `
    <div class="sr-item" onclick="openGame('${g.id}');searchResults.classList.remove('open');searchInput.value=''">
      <span>${g.emoji}</span> ${g.title}
    </div>
  `).join('') : '<div style="color:var(--muted);font-size:13px">No games found.</div>';
  searchResults.classList.add('open');
});

document.addEventListener('click', function(e) {
  if (!e.target.closest('.search-bar') && !e.target.closest('.search-results')) {
    searchResults.classList.remove('open');
  }
});

document.addEventListener('keydown', function(e) {
  if (e.key === 'Escape') { closeModal(); searchResults.classList.remove('open'); }
});
</script>
</body>
</html>
